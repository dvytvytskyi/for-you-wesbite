# 📊 Аналіз інтеграції Properties API з фронтендом

## 🔍 Порівняння: Backend API vs Frontend Implementation

---

## 1. ФІЛЬТРИ

### ✅ Що є на фронтенді:
- ✅ `propertyType` (off-plan / secondary) - є toggle
- ✅ `search` - є поле пошуку
- ✅ `bedrooms` - є multiselect (1-6)
- ✅ `sizeFrom` / `sizeTo` - є range inputs (м² або sq.ft)
- ✅ `priceFrom` / `priceTo` - є range inputs (AED)

### ❌ Що відсутнє на фронтенді:
- ❌ **`developerId`** - фільтр по девелоперу (multiselect)
- ❌ **`cityId`** - фільтр по місту (multiselect)
- ❌ **`areaId`** - фільтр по району (multiselect) - зараз є тільки мок дані
- ❌ **`countryId`** - фільтр по країні (якщо потрібно)

### 🔧 Що потрібно виправити:
1. **Location filter** - зараз використовує мок дані, потрібно:
   - Завантажувати `areas` з API (`/api/public/data`)
   - Підтримувати multiselect для `areaId`
   - Додати фільтри для `cityId` та `developerId`

2. **Bedrooms filter логіка**:
   - Для `off-plan`: перевіряти `bedroomsFrom <= bedrooms <= bedroomsTo`
   - Для `secondary`: перевіряти `bedrooms = bedrooms`
   - Зараз на фронтенді просто передає масив чисел

3. **Size/Price units**:
   - Backend використовує м² для `sizeFrom/sizeTo`
   - Frontend показує м² або sq.ft, але передає в м² - **ПЕРЕВІРИТИ**

4. **Price**:
   - Backend очікує USD для `priceFrom/priceTo`
   - Frontend показує AED - **ПОТРІБНО КОНВЕРТУВАТИ** (USD = AED / 3.67)

---

## 2. СОРТУВАННЯ

### ✅ Що є на фронтенді:
- ✅ Dropdown для сортування
- ✅ Опції:
  - `price-desc` / `price-asc` - ціна (вище/нижче)
  - `size-desc` / `size-asc` - розмір (більше/менше)
  - `newest` - нові спочатку

### ❌ Що відсутнє на фронтенді:
- ❌ **Правильне мапування значень сортування**:
  - Frontend: `price-desc` → Backend: `sortBy=priceFrom&sortOrder=DESC`
  - Frontend: `price-asc` → Backend: `sortBy=priceFrom&sortOrder=ASC`
  - Frontend: `size-desc` → Backend: `sortBy=sizeFrom&sortOrder=DESC` (для off-plan) або `sortBy=size&sortOrder=DESC` (для secondary)
  - Frontend: `size-asc` → Backend: `sortBy=sizeFrom&sortOrder=ASC` (для off-plan) або `sortBy=size&sortOrder=ASC` (для secondary)
  - Frontend: `newest` → Backend: `sortBy=createdAt&sortOrder=DESC`

- ❌ **Сортування по назві** (`sortBy=name`)

### 🔧 Що потрібно виправити:
1. Створити функцію мапування frontend sort → backend params:
   ```typescript
   const mapSortToBackend = (frontendSort: string, propertyType: 'off-plan' | 'secondary') => {
     const mapping = {
       'price-desc': { sortBy: propertyType === 'off-plan' ? 'priceFrom' : 'price', sortOrder: 'DESC' },
       'price-asc': { sortBy: propertyType === 'off-plan' ? 'priceFrom' : 'price', sortOrder: 'ASC' },
       'size-desc': { sortBy: propertyType === 'off-plan' ? 'sizeFrom' : 'size', sortOrder: 'DESC' },
       'size-asc': { sortBy: propertyType === 'off-plan' ? 'sizeFrom' : 'size', sortOrder: 'ASC' },
       'newest': { sortBy: 'createdAt', sortOrder: 'DESC' },
     };
     return mapping[frontendSort] || mapping['newest'];
   };
   ```

2. Додати опцію сортування по назві (A-Z, Z-A)

---

## 3. ПОШУК

### ✅ Що є на фронтенді:
- ✅ Поле пошуку (`search`)

### ❌ Що відсутнє / потрібно перевірити:
- ❌ Перевірити чи правильно передається параметр `search` в API запит
- ❌ Зараз пошук йде в реальному часі (onChange) - можливо потрібен debounce

---

## 4. ІНФОРМАЦІЯ КАРТОК (Список проектів)

### ✅ Що є на картці:
- ✅ `id` - для переходу
- ✅ `name` / `nameRu` - назва
- ✅ `images[0]` - перше фото
- ✅ `location.area` / `location.city` - локація
- ✅ `price.aed` - ціна в AED
- ✅ `bedrooms` / `bathrooms` - кількість
- ✅ `size.sqm` / `size.sqft` - розмір
- ✅ `developer.name` - девелопер
- ✅ `type` - тип (new/secondary)

### ❌ Що відсутнє на картці:
1. **Різниця між off-plan та secondary**:
   - **Off-plan картка повинна показувати:**
     - `priceFrom` / `priceFromAED` (замість `price`)
     - `bedroomsFrom - bedroomsTo` (замість `bedrooms`)
     - `bathroomsFrom - bathroomsTo` (замість `bathrooms`)
     - `sizeFrom - sizeTo` / `sizeFromSqft - sizeToSqft` (замість `size`)
   - **Secondary картка повинна показувати:**
     - `price` / `priceAED`
     - `bedrooms`
     - `bathrooms`
     - `size` / `sizeSqft`

2. **Відсутні поля:**
   - ❌ `country.nameEn/Ru/Ar` - країна
   - ❌ `developer.logo` - логотип девелопера
   - ❌ `facilities[]` - зручності (можна показати іконки)

3. **Структура даних:**
   - Frontend використовує мок структуру даних
   - Потрібно адаптувати під реальну структуру з API

### 🔧 Що потрібно виправити:
1. Оновити інтерфейс `Property` в `PropertyCard.tsx`:
   ```typescript
   interface Property {
     id: string;
     propertyType: 'off-plan' | 'secondary';
     name: string; // (без nameRu - бекенд повертає nameEn/nameRu/nameAr)
     description: string;
     photos: string[]; // (не images)
     country: { id: string; nameEn: string; nameRu: string; nameAr: string; code: string; };
     city: { id: string; nameEn: string; nameRu: string; nameAr: string; };
     area: { id: string; nameEn: string; nameRu: string; nameAr: string; };
     developer: { id: string; name: string; logo: string | null; };
     latitude: number;
     longitude: number;
     
     // Off-plan fields
     priceFrom?: number;
     priceFromAED?: number;
     bedroomsFrom?: number;
     bedroomsTo?: number;
     bathroomsFrom?: number;
     bathroomsTo?: number;
     sizeFrom?: number;
     sizeFromSqft?: number;
     sizeTo?: number;
     sizeToSqft?: number;
     
     // Secondary fields
     price?: number;
     priceAED?: number;
     bedrooms?: number;
     bathrooms?: number;
     size?: number;
     sizeSqft?: number;
     
     facilities: Array<{ id: string; nameEn: string; nameRu: string; nameAr: string; iconName: string; }>;
   }
   ```

2. Оновити `PropertyCard.tsx` для підтримки обох типів

---

## 5. ФОТО

### ✅ Що є на фронтенді:
- ✅ Показ фото в картці (`images[0]`)
- ✅ Слайд-шоу фото в картці (якщо більше одного)
- ✅ Галерея фото на детальній сторінці

### ❌ Що відсутнє / потрібно перевірити:
- ❌ Поле називається `photos` в API, а на фронтенді `images` - **ПОТРІБНО ПЕРЕЙМЕНУВАТИ**
- ❌ Перевірити чи правильно відображаються фото з Cloudinary URL

---

## 6. ДЕТАЛІ КОНКРЕТНОГО ПРОЕКТУ

### ✅ Що є на сторінці деталей:
- ✅ `name` - назва
- ✅ `description` - опис
- ✅ `photos[]` - галерея фото
- ✅ `location` (area, city) - локація
- ✅ `developer.name` - девелопер
- ✅ `facilities[]` - зручності
- ✅ `units[]` - юніти (для off-plan)
- ✅ `priceFrom` / `price` - ціна
- ✅ `sizeFrom-sizeTo` / `size` - розмір
- ✅ `bedroomsFrom-bedroomsTo` / `bedrooms` - спальні
- ✅ `bathroomsFrom-bathroomsTo` / `bathrooms` - ванні
- ✅ `latitude` / `longitude` - карта
- ✅ `paymentPlan` - план оплати (для off-plan)

### ❌ Що відсутнє на сторінці деталей:
1. **Детальна інформація про район:**
   - ❌ `area.description` (title + description)
   - ❌ `area.infrastructure` (title + description)
   - ❌ `area.images[]` - фото району

2. **Детальна інформація про девелопера:**
   - ❌ `developer.description` - опис девелопера
   - ❌ `developer.images[]` - фото девелопера
   - ❌ `developer.logo` - логотип (якщо є)

3. **Юніти (units) - для off-plan:**
   - ✅ Є показ юнітів
   - ❌ Перевірити чи правильно відображаються:
     - `unitId` - ID юніту
     - `type` - тип юніту (apartment, villa, penthouse, townhouse, office)
     - `price` / `priceAED` - ціна
     - `totalSize` / `totalSizeSqft` - розмір
     - `balconySize` / `balconySizeSqft` - розмір балкону
     - `planImage` - план юніту

4. **Валютна конвертація:**
   - ❌ Перевірити чи правильно показуються ціни в USD/AED
   - ❌ Перевірити чи правильно показуються розміри в м²/sqft

5. **Структура даних:**
   - Frontend використовує мок структуру
   - Потрібно адаптувати під реальну структуру з API

---

## 7. ПРИЙОМ ЗАЯВКИ ЗІ СТОРІНКИ ПРОЕКТУ

### ❌ Що повністю відсутнє:
1. **Форма заявки на сторінці деталей:**
   - ❌ Зараз є тільки мок форма (рядок 628-691 в PropertyDetail.tsx)
   - ❌ Форма не відправляє дані на API
   - ❌ Форма не має всіх необхідних полів

2. **Поля форми (за API):**
   - ❌ `propertyId` - ID проекту (автоматично)
   - ❌ `amount` - сума інвестиції (USD) - **ПОТРІБНО ДОДАТИ**
   - ❌ `date` - дата інвестиції - **ПОТРІБНО ДОДАТИ**
   - ❌ `notes` - додаткові нотатки - **ПОТРІБНО ДОДАТИ**
   - ✅ `userEmail` - є в формі
   - ✅ `userPhone` - є в формі
   - ✅ `userFirstName` - є в формі
   - ✅ `userLastName` - є в формі

3. **Логіка форми:**
   - ❌ Якщо користувач зареєстрований (є JWT token):
     - Не показувати поля `userEmail`, `userPhone`, `userFirstName`, `userLastName`
     - Використовувати `POST /api/investments` (з JWT)
   - ❌ Якщо користувач не зареєстрований:
     - Показувати всі поля
     - Використовувати `POST /api/investments/public` (без JWT)

4. **Валідація:**
   - ❌ `propertyId` - обов'язкове
   - ❌ `amount` - обов'язкове, > 0
   - ❌ `date` - обов'язкове, валідна дата
   - ❌ `userEmail` - обов'язкове якщо не зареєстрований, формат email
   - ❌ `userPhone` - обов'язкове якщо не зареєстрований
   - ❌ `userFirstName` - обов'язкове якщо не зареєстрований
   - ❌ `userLastName` - обов'язкове якщо не зареєстрований

5. **Після відправки:**
   - ❌ Показати повідомлення про успіх
   - ❌ Обробити помилки
   - ❌ Можливо показувати статус заявки (`pending`, `confirmed`, `completed`, `cancelled`)

### 🔧 Що потрібно зробити:
1. Створити компонент `InvestmentForm.tsx` з усіма полями
2. Додати валідацію (react-hook-form + zod)
3. Реалізувати логіку для зареєстрованих/незареєстрованих користувачів
4. Підключити API endpoints:
   - `POST /api/investments/public` (для не зареєстрованих)
   - `POST /api/investments` (для зареєстрованих)
5. Додати обробку помилок та успішних відповідей

---

## 📋 ПІДСУМОК: ЩО ПОТРІБНО ЗРОБИТИ

### Пріоритет 1 (Критично важливо):
1. ✅ Створити API клієнт для запитаів
2. ✅ Оновити структуру даних `Property` під реальну API
3. ✅ Додати фільтри: `developerId`, `cityId`, `areaId` (завантажувати з API)
4. ✅ Виправити мапування сортування (frontend → backend)
5. ✅ Виправити конвертацію валют (AED → USD для API)
6. ✅ Адаптувати картки для off-plan vs secondary
7. ✅ Створити форму заявки з усіма полями та API інтеграцією

### Пріоритет 2 (Важливо):
8. ✅ Завантажувати `areas`, `cities`, `developers` з API для фільтрів
9. ✅ Додати відображення деталей району та девелопера на сторінці проекту
10. ✅ Перевірити та виправити відображення юнітів
11. ✅ Додати debounce для пошуку
12. ✅ Додати сортування по назві

### Пріоритет 3 (Бажано):
13. ✅ Додати пагінацію (якщо API підтримує)
14. ✅ Додати loading states
15. ✅ Додати error handling
16. ✅ Оптимізувати завантаження фото

---

## 🔗 API Endpoints для використання:

1. **GET /api/properties** - список проектів (з фільтрами, сортуванням, пошуком)
2. **GET /api/properties/:id** - деталі проекту
3. **GET /api/public/data** - всі дані (areas, cities, developers, facilities)
4. **POST /api/investments/public** - заявка (не зареєстрований)
5. **POST /api/investments** - заявка (зареєстрований, з JWT)

---

## 📝 Примітки:

- Всі ціни в API в USD, але автоматично конвертуються в AED
- Всі розміри в API в м², але автоматично конвертуються в sqft
- Для off-plan використовуються поля з `From/To` (діапазон)
- Для secondary використовуються точні значення
- Authentication: JWT Token або API Key (x-api-key + x-api-secret)

